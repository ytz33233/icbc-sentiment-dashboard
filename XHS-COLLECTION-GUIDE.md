# 小红书舆情采集任务说明

## 一、采集目标

**平台**：小红书（Xiaohongshu）  
**聚焦**：工商银行运营活动相关的客户反馈与舆情  
**用途**：汇总到舆情监测看板，供业务团队评估活动口碑、识别风险

---

## 二、搜索关键词（8组）

必须覆盖以下关键词组合进行搜索采集：

| 序号 | 关键词组合 | 说明 |
|------|-----------|------|
| 1 | `工行 升金有礼` | 核心活动 |
| 2 | `工银i豆` | i豆活动 |
| 3 | `工行 i豆活动` | i豆活动变体 |
| 4 | `工行 升金有礼 投诉` | 投诉类 |
| 5 | `工行 i豆 投诉` | i豆投诉 |
| 6 | `工行 活动 虚假宣传` | 虚假宣传 |
| 7 | `工行 谢谢参与` | 抽奖反馈 |
| 8 | `工行 积分 清零` | 积分相关 |

> 注意：每组关键词独立搜索，结果合并去重。标题或正文必须同时包含「工行/工银/工商银行」和活动相关词才算有效。

---

## 三、什么样的舆情**不要**（过滤规则）

### ❌ 1. 互助帖（优先过滤）

用户互相点赞、关注、组队的低价值内容：

```
互点、互赞、互关、搭子、互助、互粉、互刷、互评、
互收藏、求互、来互、互的、互一下、组队、组队做、
组队互、拉人、拉新、助力、帮点、互相点、抱团、互暖
```

**示例**："工银i豆互助！找长期搭子！" → **丢弃**

### ❌ 2. 广告/营销帖

明显商业推广、非真实用户反馈：

```
推广、广告、代运营、代发、接单、接广告、商务合作、
软文、刷量、买粉、买赞、水军、代刷、代做任务
```

**示例**："扫码领大额优惠券，工行活动代理" → **丢弃**

### ❌ 3. 非工行内容（竞品信息）

**条件**：不含「工行/工银/工商银行/ICBC」，但含其他银行名称：

```
农业银行、农行、建设银行、建行、中国银行、中行、
招商银行、招行、邮储银行、交通银行、交行、
中信银行、光大银行、浦发银行、平安银行、兴业银行...
```

**示例**："建行升金有礼真不错" → **丢弃**（不含工行关键词）

### ❌ 4. 员工内部吐槽

非客户反馈，而是银行员工内部讨论：

```
员工出道、打工人、银行员工吐槽、员工辞职、
员工离职、员工日常、银行人的日常
```

**示例**："工商银行员工出道了" → **丢弃**

### ❌ 5. 过旧内容（>30天）

发布时间超过 30 天的历史内容不再纳入当日报表。

---

## 四、数据格式要求

每条舆情记录必须包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 唯一标识，如 `20260520-01` |
| `title` | string | 笔记标题（前40字，超出截断加...） |
| `content` | string | 笔记正文摘要（纯文本，去HTML标签） |
| `author` | string | 作者昵称 |
| `sourceType` | string | 固定为 `xiaohongshu` |
| `source` | string | 作者昵称或"小红书用户" |
| `url` | string | 笔记链接 |
| `date` | string | 发布日期 `YYYY-MM-DD` |
| `publishTime` | string | 完整发布时间 |
| `fetchTime` | string | 采集日期 `YYYY-MM-DD` |
| `sentiment` | string | `positive` / `negative` / `neutral` |
| `riskLevel` | string | `high` / `medium` / `low` |
| `recency` | string | `24h内` 或 `历史` |
| `likes` | number | 点赞数 |
| `comments` | number | 评论数 |
| `favorites` | number | 收藏数 |
| `shares` | number | 分享数（如有） |
| `heatScore` | number | 热度分 = likes + comments×2 + favorites + shares×3 |
| `keywords` | array | 提取的关键词（最多5个） |
| `category` | string | 分类：i豆兑换/薅羊毛/资产达标/投诉维权/虚假宣传/其他 |
| `relatedProduct` | string | 相关产品：升金有礼/i豆活动/心动有礼/i豆乐园/其他 |

---

## 五、情感判定规则

### 🔴 负面（negative）

标题或正文包含以下负面词：

```
投诉、维权、虚假宣传、空奖、骗、坑、垃圾、差、烂、
被骗、套路、恶心、失望、愤怒、差评、吐槽、坑人、
忽悠、诈骗、假货、不满、后悔、坑爹、坑死、无语、
气死、流氓、黑幕、曝光、谢谢参与（单独出现为负面）
```

**例外规则**：对比句式中夸工行的算正面，如：  
"建行...谢谢参与，**而工行直接给了我**...立减金" → **positive**

### 🟢 正面（positive）

包含以下正向词：

```
好评、不错、推荐、赞、满意、给力、棒、好用、划算、
超值、完美、优秀、惊喜、开心、高兴、愉快、实惠、
羊毛、攻略、必中
```

### ⚪ 中性（neutral）

不包含明显正面或负面词的内容，如活动通知、攻略分享等。

---

## 六、分类规则

| 分类 | 触发关键词 |
|------|-----------|
| `i豆兑换` | i豆、积分、兑换、商城、美团、立减金 |
| `薅羊毛` | 羊毛、线报、赚客、红包、优惠券、代金券、抽奖、薅 |
| `资产达标` | 升金有礼、资产达标、月月升金、资产提升、心动有礼 |
| `投诉维权` | 投诉、维权、黑猫、95588、客服、不发货、冻结 |
| `虚假宣传` | 虚假宣传、谢谢参与、空奖、奖励不符、霸王条款 |
| `其他` | 未命中以上分类 |

---

## 七、输出要求

1. **文件格式**：JSON，保存为 `xhs-YYYY-MM-DD.json`
2. **编码**：UTF-8
3. **结构**：
   ```json
   {
     "reportDate": "2026-05-20",
     "generatedAt": "2026-05-20 14:00",
     "summary": { "total": 20, "negativeCount": 2, ... },
     "records": [ {...}, {...} ]
   }
   ```
4. **去重**：同一笔记 ID 或同一 URL 只保留一条
5. **数量**：每组关键词至少采集前 10 条，合并后去重过滤

---

## 八、质量检查清单

采集完成后自检：

- [ ] 所有记录 `sourceType` 是否为 `xiaohongshu`
- [ ] 是否已过滤互助帖、广告、非工行内容
- [ ] 情感判定是否合理（抽查 5 条）
- [ ] URL 是否完整可访问
- [ ] 互动数据（点赞/评论/收藏）是否为数字
- [ ] 总数是否与客户实际讨论量相符（异常少/多需排查）
